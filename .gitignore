# Gradle build outputs
app/build/
build/
.gradle/
*.class
*.dex

# Local SDK path (machine-specific)
local.properties

# Android Studio
.idea/
*.iml
.cxx/

# Local staged Valve / native binaries (recreated via tools script)
app/src/main/jniLibs/arm64-v8a/*.so

# OS
.DS_Store
Thumbs.db
